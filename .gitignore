# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# uv
# Note: uv.lock is intentionally tracked — it pins all transitive deps for reproducibility.
# Uncomment the line below only if you deliberately choose not to commit the lock file.
# uv.lock

# OS
.DS_Store
Thumbs.db

# Secrets
.env
.env.*

# Project
*.sqlite
*.db
*.jsonl
*.npz
*.log
results/
logs/
/traces/
# (the package src/hope/traces/ is intentionally tracked; the rule
# above is anchored with a leading slash so only the top-level
# `traces/` runtime-output directory is ignored.)
coding_task_*
get-pip.py
# Junk from mocked-path tests that write to their mock's __repr__ as a path
MagicMock/

# MkDocs build output
site/

# Frontend
frontend/node_modules/
frontend/dist/
src/hope/server/static/

# Desktop (Tauri)
frontend/src-tauri/target/

# Worktrees
.worktrees/

# Rust
target/

# Claude plan artifacts
docs/plans/
docs/superpowers/
.superpowers/

# Claude Code project instructions (per-developer)
CLAUDE.md
.claude/

# Tauri auto-generated schemas
**/src-tauri/gen/schemas/

# NFS lock artifacts
.nfs*
**/.nfs*

# Research output
research_mining_*
.python-version
src/hope/channels/whatsapp_baileys_bridge/node_modules/

# SQLite in-memory artifacts
:memory:

# Second Repos
Inline/
scratch/

# Local agent state — pure runtime, regenerable, never commit
.claude-flow/
.swarm/
.hope-io/
agentdb.rvf
agentdb.rvf.lock
ruvector.db
ruvector.db-*
*.db-shm
*.db-wal
